language: php

php:
  - 5.5

env:
    - WP_VERSION=latest WP_MULTISITE=0

addons:
  hosts:
    - local.wordpress.dev

before_install:
  - sudo apt-get update > /dev/null
  - ./ci/prepare.sh

install:


before_script:

script:

  # Lint 'n 'hint
  - make lint

notifications:

  slack: a8c:rdeQR68xgiffkssb1ZNw5P1c